import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#003B73",
    },
    secondary: {
      main: "#FF5700",
    },
  },
  typography: {
    fontFamily: "Roboto",
  },
});

export default theme;
